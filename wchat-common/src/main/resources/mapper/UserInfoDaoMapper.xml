<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.longjunwang.wchatcommon.mapper.UserInfoMapper">

    <resultMap id="userInfo" type="com.longjunwang.wchatcommon.entity.ouath.UserInfo">
        <id property="id" column="id" />
        <result property="userName" column="user_name" />
        <result property="avatarUrl" column="avatar_url" />
        <result property="phone" column="phone" />
        <result property="email" column="email" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <select id="selectByEmail" parameterType="string" resultMap="userInfo">
        select * from user_info where email = #{email}
    </select>
    <select id="selectAll" resultMap="userInfo">
        select * from user_info;
    </select>
</mapper>