<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.longjunwang.wchatcommon.mapper.FriendRelationMapper">

    <resultMap id="userInfo" type="com.longjunwang.wchatcommon.entity.FriendRelation">
        <result property="userId" column="user_id" />
        <result property="friendId" column="friend_id" />
        <result property="createTime" column="create_time" />
        <result property="updateTime" column="update_time" />
    </resultMap>
    <insert id="insert" parameterType="com.longjunwang.wchatcommon.entity.FriendRelation">
        insert into friend_relation
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                user_id,
            </if>
            <if test="friendId != null">
                friend_id,
            </if>
            <if test="createTime != null">
                create_time,
            </if>
            <if test="updateTime != null">
                update_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{userId},
            </if>
            <if test="userName != null">
                #{friendId},
            </if>
            <if test="createTime != null">
                #{createTime},
            </if>
            <if test="updateTime != null">
                #{updateTime},
            </if>
        </trim>
    </insert>

    <select id="selectOneRelation" resultType="com.longjunwang.wchatcommon.entity.FriendRelation">
        select * from friend_relation where user_id = #{userId} and friend_id = #{friendId}
    </select>
</mapper>